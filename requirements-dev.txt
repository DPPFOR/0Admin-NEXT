# Entwicklungs- und Test-Abhängigkeiten (lokal & CI).
# Diese Pakete werden in der Produktion nicht benötigt.

pytest>=8,<9
pytest-asyncio>=0.23,<1

# Lint/Format optional (wenn gewünscht)
ruff>=0.6,<1
black>=24.8,<25
